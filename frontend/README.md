# HR Bot Frontend

Админ-панель для управления опросами и сотрудниками в HR Bot.

## 📋 Описание

React приложение для управления опросами, сотрудниками и просмотра результатов. Позволяет HR-менеджерам создавать опросы, управлять сотрудниками и анализировать ответы.

## 🛠 Технологии

### Основные технологии
- **React 18** — UI библиотека
- **TypeScript** — типобезопасность
- **Vite** — быстрый сборщик
- **React Router DOM** — роутинг
- **TanStack React Query** — управление состоянием и кэширование
- **Axios** — HTTP клиент
- **React Hook Form** — управление формами
- **Zod** — валидация схем

### UI Компоненты
- **Tailwind CSS** — стилизация
- **shadcn/ui** — компоненты интерфейса (опционально)

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/               # React компоненты
│   │   ├── surveys/              # Компоненты для опросов
│   │   │   ├── index.ts
│   │   │   ├── QuestionAdder.tsx # Добавление вопросов
│   │   │   └── QuestionEditor.tsx # Редактирование вопросов
│   │   ├── employees/            # Компоненты для сотрудников
│   │   │   └── EmployeeForm.tsx   # Форма сотрудника
│   │   ├── results/              # Компоненты для результатов
│   │   │   └── AnswerViewer.tsx   # Просмотр ответов
│   │   └── ui/                   # UI компоненты
│   │       ├── index.ts
│   │       ├── Button.tsx
│   │       ├── Input.tsx
│   │       ├── Select.tsx
│   │       ├── Modal.tsx
│   │       ├── Card.tsx
│   │       ├── Table.tsx
│   │       ├── Badge.tsx
│   │       └── Status.tsx
│   ├── pages/                    # Страницы приложения
│   │   ├── Dashboard.tsx          # Панель управления
│   │   ├── Surveys.tsx            # Список опросов
│   │   ├── Employees.tsx          # Список сотрудников
│   │   ├── Results.tsx            # Все ответы
│   │   └── SurveyResults.tsx      # Результаты конкретного опроса
│   ├── services/                 # API клиент
│   │   └── api.ts                 # Axios инстанс
│   ├── types/                    # TypeScript типы
│   │   └── index.ts               # Общие типы
│   ├── App.tsx                    # Главный компонент с роутингом
│   ├── main.tsx                   # Точка входа
│   └── index.css                  # Глобальные стили
├── package.json                  # Зависимости Node.js
├── tsconfig.json                  # TypeScript конфигурация
├── tsconfig.node.json             # TypeScript конфигурация для Node
├── vite.config.ts                 # Vite конфигурация
└── .env                           # Переменные окружения
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:8000/api
```

### 3. Запуск разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### 4. Сборка для продакшена

```bash
npm run build
```

Сборка будет создана в директории `dist/`.

## 📚 API Интеграция

### API клиент

Приложение использует Axios для HTTP запросов к backend API.

**Файл**: `src/services/api.ts`

```typescript
// Пример использования
import api from './api';

// Получить все опросы
const surveys = await api.get('/surveys');

// Создать опрос
const newSurvey = await api.post('/surveys', surveyData);

// Получить сотрудника по ID
const employee = await api.get(`/employees/${id}`);
```

### Типы данных

Все типы определены в `src/types/index.ts`:

```typescript
interface Survey {
  id: number;
  title: string;
  description: string;
  days_after_start: number;
  is_active: boolean;
  questions: Question[];
}

interface Employee {
  id: number;
  telegram_id: number;
  telegram_username: string;
  first_name: string;
  last_name: string;
  start_date: string;
  is_active: boolean;
}
```

## 📄 Страницы приложения

### Dashboard (Панель управления)

**Файл**: `src/pages/Dashboard.tsx`

Отображает общую статистику:
- Общее количество опросов
- Общее количество сотрудников
- Общее количество ответов
- Статистика по опросам

### Surveys (Опросы)

**Файл**: `src/pages/Surveys.tsx`

Функционал:
- Создание нового опроса
- Редактирование существующих опросов
- Удаление опросов
- Просмотр списка опросов
- Переключение статуса (активный/неактивный)

**Компоненты**:
- `QuestionAdder.tsx` — добавление вопросов
- `QuestionEditor.tsx` — редактирование вопросов

### Employees (Сотрудники)

**Файл**: `src/pages/Employees.tsx`

Функционал:
- Создание новых сотрудников
- Редактирование информации о сотрудниках
- Удаление сотрудников
- Просмотр списка сотрудников

**Компоненты**:
- `EmployeeForm.tsx` — форма для создания/редактирования

### Results (Все ответы)

**Файл**: `src/pages/Results.tsx`

Функционал:
- Просмотр всех ответов на опросы
- Фильтрация по опросам
- Просмотр деталей ответов

### SurveyResults (Результаты опроса)

**Файл**: `src/pages/SurveyResults.tsx`

Функционал:
- Детальный просмотр результатов конкретного опроса
- Просмотр ответов по каждому сотруднику
- Статистика по ответам
- Приглашение сотрудников к опросу

## 🎨 UI Компоненты

Приложение использует переиспользуемые UI компоненты:

### Button

```tsx
<Button variant="primary" onClick={handleClick}>
  Сохранить
</Button>
```

Варианты: `primary`, `secondary`, `danger`, `ghost`

### Input

```tsx
<Input
  label="Название"
  value={value}
  onChange={handleChange}
  placeholder="Введите название"
/>
```

### Modal

```tsx
<Modal isOpen={isOpen} onClose={onClose} title="Подтверждение">
  <p>Вы уверены?</p>
  <Button onClick={onConfirm}>Да</Button>
</Modal>
```

### Table

```tsx
<Table
  columns={columns}
  data={data}
  onRowClick={handleRowClick}
/>
```

### Badge

```tsx
<Badge variant="success">Активен</Badge>
<Badge variant="warning">Ожидает</Badge>
<Badge variant="danger">Неактивен</Badge>
```

## 🔄 Работа с данными

### React Query

Приложение использует TanStack React Query для управления состоянием:

```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';

// Получение данных
const { data, isLoading, error } = useQuery({
  queryKey: ['surveys'],
  queryFn: () => api.get('/surveys'),
});

// Создание данных
const mutation = useMutation({
  mutationFn: (data) => api.post('/surveys', data),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['surveys'] });
  },
});
```

### Валидация форм

Используется React Hook Form с Zod схемами:

```typescript
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import * as z from 'zod';

const schema = z.object({
  title: z.string().min(1, 'Обязательное поле'),
  description: z.string().optional(),
});

const form = useForm({
  resolver: zodResolver(schema),
});
```

## 🎯 Примеры использования

### Создание опроса

```typescript
import { useMutation } from '@tanstack/react-query';
import api from '../services/api';

function useCreateSurvey() {
  return useMutation({
    mutationFn: (surveyData) => api.post('/surveys', surveyData),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['surveys'] });
    },
  });
}

// Использование
const createSurvey = useCreateSurvey();

const handleSubmit = async (data) => {
  await createSurvey.mutateAsync(data);
};
```

### Получение сотрудников

```typescript
import { useQuery } from '@tanstack/react-query';
import api from '../services/api';

function useEmployees() {
  return useQuery({
    queryKey: ['employees'],
    queryFn: () => api.get('/employees'),
  });
}

// Использование
const { data: employees, isLoading } = useEmployees();

if (isLoading) return <div>Загрузка...</div>;

return (
  <div>
    {employees?.map(employee => (
      <div key={employee.id}>
        {employee.first_name} {employee.last_name}
      </div>
    ))}
  </div>
);
```

## 🛠 Разработка

### Запуск в режиме разработки

```bash
# Backend (в другом терминале)
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Frontend
cd frontend
npm run dev
```

### Структура компонентов

**Компоненты верхнего уровня** (`src/pages/`):
- `Dashboard.tsx` — главная страница
- `Surveys.tsx` — управление опросами
- `Employees.tsx` — управление сотрудниками
- `Results.tsx` — все ответы
- `SurveyResults.tsx` — результаты конкретного опроса

**Компоненты переиспользуемые** (`src/components/`):
- `ui/` — базовые UI компоненты
- `surveys/` — компоненты для опросов
- `employees/` — компоненты для сотрудников
- `results/` — компоненты для результатов

### Добавление новых компонентов

1. Создайте компонент в соответствующей директории
2. Используйте TypeScript для типобезопасности
3. Применяйте хуки React для управления состоянием
4. Используйте React Query для работы с данными
5. Добавьте стили через Tailwind CSS

## 🧪 Тестирование

### Запуск тестов

```bash
npm test
```

### Запуск тестов в watch режиме

```bash
npm run test:watch
```

## 📦 Сборка

### Сборка для продакшена

```bash
npm run build
```

### Предпросмотр сборки

```bash
npm run preview
```

## 🎨 Стилизация

Приложение использует Tailwind CSS для стилизации. Все компоненты стилизуются через utility classes.

### Пример стилизации

```tsx
<div className="p-4 bg-white rounded-lg shadow-md">
  <h2 className="text-xl font-semibold mb-2">Заголовок</h2>
  <p className="text-gray-600">Описание</p>
</div>
```

## 🔧 Конфигурация

### TypeScript

Файл `tsconfig.json` содержит конфигурацию TypeScript для проекта.

### Vite

Файл `vite.config.ts` содержит конфигурацию Vite для сборки.

### Proxy

В `vite.config.ts` настроен proxy для API запросов:

```typescript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
});
```

## 📝 TODO

- [ ] Добавить аутентификацию
- [ ] Добавить графики и визуализацию результатов
- [ ] Добавить экспорт результатов в CSV/Excel
- [ ] Добавить поиск и фильтрацию
- [ ] Добавить пагинацию для больших списков
- [ ] Добавить Dark mode
- [ ] Добавить локализацию (i18n)
- [ ] Добавить unit тесты
- [ ] Добавить E2E тесты

## 📄 Лицензия

MIT License
