# HR Bot - Telegram бот для опросов сотрудников

Telegram бот для проведения опросов сотрудников через 90 дней после начала работы. Позволяет HR-менеджерам создавать опросы, управлять сотрудниками и получать результаты в удобном формате.

## 📋 Описание проекта

HR Bot — это комплексное решение для автоматизации опросов сотрудников в компаниях. Основные возможности:

- ✅ Создание опросов с различными типами вопросов
- ✅ Автоматическое определение сотрудников, прошедших 90 дней работы
- ✅ Интеграция с Telegram для удобного прохождения опросов
- ✅ Админ-панель для управления опросами и сотрудниками
- ✅ Детальная аналитика и результаты опросов
- ✅ Напоминания о незавершенных опросах

## 🛠 Технологии

### Backend
- **Python 3.11+**
- **FastAPI** — высокопроизводительный веб-фреймворк
- **Aiogram 3.x** — асинхронный фреймворк для Telegram ботов
- **SQLAlchemy** — ORM для работы с базой данных
- **SQLite** — легковесная база данных
- **Pydantic** — валидация данных

### Frontend
- **React 18**
- **TypeScript**
- **Vite** — быстрый сборщик
- **React Router DOM** — роутинг
- **TanStack React Query** — управление состоянием
- **Axios** — HTTP клиент

### Инструменты
- **ngrok** — туннелирование для локального тестирования
- **Docker** — контейнеризация (опционально)

## 📁 Структура проекта

```
hrbot/
├── backend/                          # Backend часть проекта
│   ├── app/
│   │   ├── main.py                   # FastAPI приложение
│   │   ├── config.py                 # Конфигурация приложения
│   │   ├── database.py               # Подключение к БД
│   │   ├── models/                   # SQLAlchemy модели
│   │   │   ├── employee.py           # Модель сотрудника
│   │   │   ├── survey.py             # Модель опроса
│   │   │   └── response.py           # Модель ответа
│   │   ├── schemas/                  # Pydantic схемы
│   │   │   ├── employee.py           # Схемы для сотрудников
│   │   │   ├── survey.py             # Схемы для опросов
│   │   │   └── response.py           # Схемы для ответов
│   │   ├── api/v1/                   # API endpoints
│   │   │   ├── surveys.py            # Опросы
│   │   │   ├── employees.py          # Сотрудники
│   │   │   ├── responses.py          # Ответы
│   │   │   └── bot.py                # Telegram бот API
│   │   ├── bot/                      # Aiogram бот
│   │   │   ├── bot.py                # Основной бот
│   │   │   ├── handlers/             # Обработчики сообщений
│   │   │   ├── keyboards/            # Клавиатуры бота
│   │   │   ├── services/             # Сервисы бота
│   │   │   └── fsm/                  # Машина состояний
│   │   ├── core/                     # Ядро приложения
│   │   └── utils/                    # Утилиты
│   ├── requirements.txt              # Зависимости Python
│   ├── .env.example                  # Пример конфигурации
│   └── hrbot.db                      # База данных (создается автоматически)
├── frontend/                         # Frontend часть проекта
│   ├── src/
│   │   ├── components/               # React компоненты
│   │   │   ├── surveys/              # Компоненты для опросов
│   │   │   ├── employees/            # Компоненты для сотрудников
│   │   │   ├── results/              # Компоненты для результатов
│   │   │   └── ui/                   # UI компоненты
│   │   ├── pages/                    # Страницы приложения
│   │   ├── services/                 # API клиент
│   │   ├── types/                    # TypeScript типы
│   │   ├── App.tsx                   # Главный компонент
│   │   ├── main.tsx                  # Точка входа
│   │   └── index.css                 # Глобальные стили
│   ├── package.json                  # Зависимости Node.js
│   └── vite.config.ts                # Vite конфигурация
├── scripts/                          # Скрипты проекта
│   ├── init_db.py                    # Инициализация БД
│   └── start_dev.sh                  # Запуск разработки
├── ARCHITECTURE.md                   # Архитектура проекта
├── API.md                            # Документация API
└── README.md                         # Этот файл
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

#### Backend (Python)

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # на Windows: venv\Scripts\activate

# Установите зависимости
cd backend
pip install -r requirements.txt
```

#### Frontend (Node.js)

```bash
cd frontend
npm install
```

### 2. Настройка переменных окружения

```bash
# Скопируйте пример конфигурации
cp backend/.env.example backend/.env

# Отредактируйте backend/.env и добавьте свои значения
```

**Необходимые переменные:**
- `BOT_TOKEN` — токен вашего Telegram бота (получить у @BotFather)
- `WEBHOOK_SECRET` — секретный ключ для валидации вебхуков
- `HR_TELEGRAM_IDS` — ID HR-сотрудников через запятую
- `API_HOST` — хост API сервера (по умолчанию: 0.0.0.0)
- `API_PORT` — порт API сервера (по умолчанию: 8000)
- `CORS_ORIGINS` — разрешенные источники для CORS
- `DATABASE_URL` — подключение к базе данных (SQLite по умолчанию)

### 3. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Следуйте инструкциям для получения токена
4. Скопируйте токен в `backend/.env` в переменную `BOT_TOKEN`

### 4. Инициализация базы данных

```bash
python scripts/init_db.py
```

Это создаст все необходимые таблицы в базе данных:
- `employees` — сотрудники
- `surveys` — опросы
- `questions` — вопросы
- `question_options` — варианты ответов
- `survey_responses` — ответы на опросы
- `answers` — детали ответов

### 5. Запуск проекта

#### Вариант A: Автоматический запуск с ngrok (рекомендуется для локальной разработки)

```bash
./scripts/start_dev.sh
```

Этот скрипт автоматически:
1. Проверяет наличие `.env` файла
2. Запускает ngrok для туннелирования
3. Настраивает webhook для Telegram
4. Запускает FastAPI сервер

#### Вариант B: Ручной запуск

**Запуск backend:**

```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Запуск frontend:**

```bash
cd frontend
npm run dev
```

#### Вариант C: Отдельно для бота

Если вы хотите запускать бота отдельно:

```bash
cd backend
python -m app.bot.bot
```

### 6. Доступ к приложению

После запуска:
- **Backend API**: http://localhost:8000
- **API Documentation (Swagger)**: http://localhost:8000/docs
- **Frontend**: http://localhost:3000

## 🔧 Использование ngrok для локального тестирования

Для тестирования webhook с локального сервера используется ngrok:

```bash
# Установите ngrok: https://ngrok.com/download
# Запустите туннель
ngrok http 8000
```

**Полученный URL** будет выглядеть как `https://abc123.ngrok.io`

**Настройка webhook:**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://<YOUR_NGROK_URL>/api/v1/bot/webhook"
```

**Отмена webhook:**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook"
```

## 📚 API Документация

Полная документация API доступна в Swagger UI при запуске сервера: http://localhost:8000/docs

### Основные эндпоинты

#### Опросы (Surveys)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/surveys` | Список всех опросов |
| POST | `/api/v1/surveys` | Создать новый опрос |
| GET | `/api/v1/surveys/{id}` | Получить опрос по ID |
| PUT | `/api/v1/surveys/{id}` | Обновить опрос |
| DELETE | `/api/v1/surveys/{id}` | Удалить опрос |

#### Сотрудники (Employees)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/employees` | Список всех сотрудников |
| POST | `/api/v1/employees` | Создать сотрудника |
| GET | `/api/v1/employees/{id}` | Получить сотрудника по ID |
| PUT | `/api/v1/employees/{id}` | Обновить сотрудника |
| DELETE | `/api/v1/employees/{id}` | Удалить сотрудника |
| GET | `/api/v1/employees/telegram/{telegram_id}` | Найти по Telegram ID |

#### Ответы (Responses)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/responses` | Список всех ответов |
| GET | `/api/v1/responses/{id}` | Получить ответ по ID |
| GET | `/api/v1/surveys/{id}/results` | Результаты опроса в JSON |

#### Telegram Bot API

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/v1/bot/webhook` | Telegram webhook endpoint |
| POST | `/api/v1/bot/initiate-survey` | Инициировать опрос для сотрудника |
| GET | `/api/v1/bot/surveys/{telegram_id}` | Список доступных опросов |
| GET | `/api/v1/bot/eligible-employees/{survey_id}` | Список подходящих сотрудников |
| POST | `/api/v1/bot/send-invite` | Отправить приглашение |
| POST | `/api/v1/bot/send-reminder` | Отправить напоминание |
| POST | `/api/v1/bot/send-reminders-batch` | Отправить batch напоминаний |

## 💡 Примеры использования

### Создание опроса через API

```bash
curl -X POST "http://localhost:8000/api/v1/surveys" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "90-Day Check-in",
    "description": "Как вам работаеться?",
    "days_after_start": 90,
    "is_active": true,
    "questions": [
      {
        "question_text": "Насколько вы удовлетворены своей ролью?",
        "question_type": "single_choice",
        "order_index": 0,
        "is_required": true,
        "options": [
          {"option_text": "Очень доволен", "order_index": 0},
          {"option_text": "Доволен", "order_index": 1},
          {"option_text": "Нейтрально", "order_index": 2},
          {"option_text": "Недоволен", "order_index": 3}
        ]
      },
      {
        "question_text": "Что бы вы хотели улучшить?",
        "question_type": "multiple_choice",
        "order_index": 1,
        "is_required": true,
        "options": [
          {"option_text": "Коммуникация", "order_index": 0},
          {"option_text": "Обучение", "order_index": 1},
          {"option_text": "Инструменты", "order_index": 2},
          {"option_text": "Организация", "order_index": 3}
        ]
      },
      {
        "question_text": "Дополнительные комментарии",
        "question_type": "text",
        "order_index": 2,
        "is_required": false
      }
    ]
  }'
```

### Регистрация сотрудника через API

```bash
curl -X POST "http://localhost:8000/api/v1/employees" \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_id": 123456789,
    "telegram_username": "johndoe",
    "first_name": "John",
    "last_name": "Doe",
    "start_date": "2025-11-10",
    "is_active": true
  }'
```

### Инициация опроса для сотрудника

```bash
curl -X POST "http://localhost:8000/api/v1/bot/initiate-survey" \
  -H "Content-Type: application/json" \
  -d '{
    "employee_telegram_id": 123456789,
    "survey_id": 1
  }'
```

### Получение результатов опроса

```bash
curl "http://localhost:8000/api/v1/surveys/1/results"
```

## 🎯 Типы вопросов

HR Bot поддерживает три типа вопросов:

1. **text** — свободный ввод ответа
   - Позволяет сотрудникам вводить произвольный текст
   - Используется для открытых вопросов и комментариев

2. **single_choice** — выбор одного варианта
   - Предоставляет список вариантов для выбора
   - Сотрудник может выбрать только один ответ

3. **multiple_choice** — выбор нескольких вариантов
   - Предоставляет список вариантов для выбора
   - Сотрудник может выбрать несколько вариантов

## 📊 Функционал бота

### Основные возможности:

- **Автоматическое приглашение** — сотрудникам отправляется приглашение через Telegram
- **Машина состояний (FSM)** — отслеживание прогресса прохождения опроса
- **Валидация ответов** — проверка обязательных полей
- **Напоминания** — отправка напоминаний о незавершенных опросах
- **Прогресс бар** — визуальное отображение прогресса прохождения
- **Клавиатуры** — удобные интерфейсы для ответов

### Workflow опроса:

1. HR создает опрос и настраивает вопросы
2. Система определяет сотрудников, прошедших 90+ дней работы
3. HR инициирует опрос для сотрудников
4. Сотрудник получает приглашение в Telegram
5. Сотрудник проходит опрос через бота
6. HR получает результаты и анализирует их

## 🛠 Разработка

### Структура кода

**Backend:**
- `app/main.py` — точка входа FastAPI приложения
- `app/api/v1/` — маршруты API
- `app/models/` — SQLAlchemy модели базы данных
- `app/schemas/` — Pydantic схемы для валидации
- `app/bot/` — логика Aiogram бота
- `app/services/` — бизнес-логика

**Frontend:**
- `src/components/` — React компоненты
- `src/pages/` — страницы приложения
- `src/services/` — API клиент
- `src/types/` — TypeScript типы

### Запуск в режиме разработки

**Backend с auto-reload:**

```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Frontend с hot-reload:**

```bash
cd frontend
npm run dev
```

## 📝 TODO

- [x] Backend с FastAPI
- [x] Telegram бот с Aiogram
- [x] CRUD для опросов, сотрудников, ответов
- [x] Frontend с React + TypeScript
- [ ] Аутентификация в админ-панели
- [ ] Автоматическая проверка 90 дней
- [ ] Напоминания о незавершенных опросах
- [ ] Экспорт результатов в CSV/Excel
- [ ] Графики и визуализация результатов
- [ ] Уведомления HR о новых ответах
- [ ] Мультиязычность
- [ ] Настройка уведомлений по email
- [ ] Интеграция с другими мессенджерами
- [ ] Docker контейнеры
- [ ] CI/CD пайплайн

## 📄 Лицензия

MIT License

## 🤝 Поддержка

Для вопросов и предложений обращайтесь через GitHub Issues.
